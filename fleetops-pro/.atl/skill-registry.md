# Skill Registry

> Generated by SDD Init — 2026-05-15
> Project: fleetops-pro (autobus)

## Source Hierarchy

| Level | Path | Status |
|-------|------|--------|
| **User skills** | `~/.config/opencode/skills/` | ✅ 35 entries |
| **Project skills** | — | ❌ none |
| **Convention files** | `AGENTS.md` | ✅ loaded |

## Included Skills

Excluded: `_shared`, `skill-registry`, `sdd-*`.

### ai-sdk-5
- **Trigger**: Building AI chat features — breaking changes from v4
- **Path**: `~/.config/opencode/skills/ai-sdk-5/`
- **Rules**:
  - `useChat` from `@ai-sdk/react`, NOT from `ai`
  - Use `new DefaultChatTransport({ api: "/api/chat" })` pattern
  - `sendMessage({ text: input })` replaces `handleSubmit` + `handleInputChange`
  - `isLoading`, `error` available from `useChat` return
  - Server: `createServerTransport()` for streaming
  - Tool calls use `tools` config in `useChat`

### angular-architecture
- **Trigger**: Structuring Angular projects or deciding where to place components
- **Path**: `~/.config/opencode/skills/angular/architecture/`
- **Rules**:
  - Scope Rule: component in `features/[name]/components/` if used by 1 feature; `features/shared/components/` if 2+
  - Main component file = folder name (`shopping-cart.ts` in `features/shopping-cart/`)
  - Feature folder: `components/`, `services/`, `models/`, `pages/`
  - Files named after export: `user-card.ts` for `UserCardComponent`
  - Types/services in `core/` not `features/`
  - Only `core/` barrel exports; feature-internal modules avoid barrels

### angular-core
- **Trigger**: Creating Angular components, using signals, or setting up zoneless
- **Path**: `~/.config/opencode/skills/angular/core/`
- **Rules**:
  - Standalone by default — omit `standalone: true`
  - Function-based IO: `input.required<T>()`, `output<T>()`, `model(false)` — never decorators
  - Signals for state: `signal()`, `computed()`, `effect()` — never RxJS `BehaviorSubject` for component state
  - `inject()` not constructor DI
  - `@if`/`@for`/`@switch` not structural directives (`*ngIf`/`*ngFor`)
  - `ChangeDetectionStrategy.OnPush` always
  - `takeUntilDestroyed()` or `destroyRef` for cleanup

### angular-forms
- **Trigger**: Working with forms, validation, or form state in Angular
- **Path**: `~/.config/opencode/skills/angular/forms/`
- **Rules**:
  - New apps → Signal Forms (`@angular/forms/signals`); production → Reactive Forms
  - Signal Forms: `form({ field: [value, validators] })`, controls via `form.controls.field`
  - `valid()`, `value()`, `touched()` are signals
  - `FormField` directive for binding
  - Reactive Forms: `FormBuilder`, `Validators`, `formGroup` directive

### angular-performance
- **Trigger**: Optimizing Angular app performance, images, or lazy loading
- **Path**: `~/.config/opencode/skills/angular/performance/`
- **Rules**:
  - Always `NgOptimizedImage` with `ngSrc` not `src`
  - Set `width` + `height` or `fill` on every image
  - `priority` on LCP image
  - `@defer (on viewport)` for lazy components; `@placeholder` and `@loading` required
  - `@defer (on interaction)` for below-fold interactive content
  - SSR: `provideClientHydration()`, `isPlatformBrowser` guard

### branch-pr
- **Trigger**: Creating, opening, or preparing PRs for review
- **Path**: `~/.config/opencode/skills/branch-pr/`
- **Rules**:
  - Every PR MUST link a `status:approved` issue — no exceptions
  - Exactly one `type:*` label per PR
  - Branch naming: `type/description` — regex `^(feat|fix|chore|docs|style|refactor|perf|test|build|ci|revert)\/[a-z0-9._-]+$`
  - Conventional commits during implementation
  - Run shellcheck on modified scripts
  - Wait for automated checks before merge

### chained-pr
- **Trigger**: PRs over 400 lines, stacked PRs, review slices
- **Path**: `~/.config/opencode/skills/chained-pr/`
- **Rules**:
  - Split at 400 changed lines unless `size:exception` approved
  - Keep ≤60 min review per PR
  - 1 deliverable work unit per PR; tests/docs stay with their code
  - Dependency diagram with `📍` marking current PR
  - Stacked PRs → each targets main; Feature Branch Chain → tracker PR + sequenced child branches
  - Clean diff per PR (rebase/retarget if polluted)

### cognitive-doc-design
- **Trigger**: Writing guides, READMEs, RFCs, onboarding, architecture, or review-facing docs
- **Path**: `~/.config/opencode/skills/cognitive-doc-design/`
- **Rules**:
  - Lead with answer/decision/outcome — context after
  - Progressive disclosure: happy path → details → edge cases → refs
  - Chunking: group related info, short flat lists
  - Signposting: headings, callouts, summaries
  - Recognition over recall: tables, checklists, templates > prose
  - Review empathy: docs designed for verifier to confirm intent without reconstructing

### comment-writer
- **Trigger**: PR feedback, issue replies, reviews, Slack messages, or GitHub comments
- **Path**: `~/.config/opencode/skills/comment-writer/`
- **Rules**:
  - Start with actionable point — no recap before feedback
  - Warm and direct like a thoughtful teammate
  - 1-3 short paragraphs or tight bullet list
  - Explain WHY when requesting changes
  - No pile-ons: comment on highest-value issue
  - Match thread language (Rioplatense Spanish if appropriate)
  - No em dashes

### django-drf
- **Trigger**: Building REST APIs with Django — ViewSets, Serializers, Filters
- **Path**: `~/.config/opencode/skills/django-drf/`
- **Rules**:
  - `ModelViewSet` for standard CRUD; `get_serializer_class()` for per-action serializers
  - `@action(detail=True, methods=["post"])` for custom endpoints
  - Separate read (expanded) and write (validated) serializers
  - `filterset_class` for filtering; `search_fields` for text search
  - `permission_classes` on ViewSet; `get_permissions()` for per-action
  - `select_related`/`prefetch_related` in `get_queryset()`

### github-pr
- **Trigger**: Creating PRs, writing PR descriptions, or using gh CLI for pull requests
- **Path**: `~/.config/opencode/skills/github-pr/`
- **Rules**:
  - PR title = conventional commit: `type(scope): short description`
  - Types: feat, fix, docs, refactor, test, chore
  - Description: Summary (1-3 bullets) → Changes → Testing checklist → `Closes #N`
  - Use `gh pr create` with body via here-string
  - Include testing evidence

### go-testing
- **Trigger**: Go tests, go test coverage, Bubbletea teatest, golden files
- **Path**: `~/.config/opencode/skills/go-testing/`
- **Rules**:
  - Table-driven tests with `t.Run(tt.name, ...)`
  - `t.TempDir()` for filesystem — never real home dir
  - `testing.Short()` guard for integration/slow tests
  - Bubbletea: `Model.Update()` for state, `teatest` only for interactive flows
  - Golden files: deterministic, update via `-update` flag, re-test without it
  - Small interfaces around system/command boundaries

### issue-creation
- **Trigger**: Creating GitHub issues, bug reports, or feature requests
- **Path**: `~/.config/opencode/skills/issue-creation/`
- **Rules**:
  - MUST use template (bug report or feature request); blank issues disabled
  - New issues get `status:needs-review` automatically
  - `status:approved` required before any PR
  - Questions go to Discussions, not issues
  - Search for duplicates before creating

### jira-epic
- **Trigger**: Creating an epic, large feature, or multi-task initiative
- **Path**: `~/.config/opencode/skills/jira-epic/`
- **Rules**:
  - Standard template: Figma link, Feature Overview, Requirements, Technical Considerations
  - Split API/UI/SDK into separate Jira stories
  - Sections by major functionality area with subsections
  - Technical Considerations section for architecture decisions

### jira-task
- **Trigger**: Creating a Jira task, ticket, or issue
- **Path**: `~/.config/opencode/skills/jira-task/`
- **Rules**:
  - Multi-component work → separate tasks per component (API, UI, SDK)
  - Bugs → sibling tasks per component
  - Features → parent task (PM-facing, no technical details) + child tasks per component
  - Description template: ACs, Technical Notes, Definition of Done

### judgment-day
- **Trigger**: Judgment day, dual review, adversarial review, `juzgar`
- **Path**: `~/.config/opencode/skills/judgment-day/`
- **Rules**:
  - Two blind judges run in parallel with identical target/criteria
  - Classify warnings: real (triggered by normal use) vs theoretical (INFO)
  - Ask before fixing Round 1 confirmed issues
  - Re-judge after every fix iteration
  - Terminal states only: `APPROVED` or `ESCALATED`
  - Max 2 fix iterations; ask user to continue after that
  - Resolve project skills from registry before launching judges

### nextjs-15
- **Trigger**: Working with Next.js — routing, Server Actions, data fetching
- **Path**: `~/.config/opencode/skills/nextjs-15/`
- **Rules**:
  - App Router files: `page.tsx`, `layout.tsx`, `loading.tsx`, `error.tsx`, `not-found.tsx`
  - Server Components are default (no `"use client"` unless needed)
  - Server Actions: `"use server"` at file or function top
  - `revalidatePath()` and `redirect()` in server actions
  - Route groups via `(folder)` — no URL impact
  - Private folders via `_folder` — not routed

### playwright
- **Trigger**: Writing E2E tests — Page Objects, selectors, MCP workflow
- **Path**: `~/.config/opencode/skills/playwright/`
- **Rules**:
  - MCP-first: navigate → snapshot → interact → screenshot → document → create test
  - Page Object Model: `tests/{page-name}/{page-name}-page.ts`
  - One spec file per page: `tests/{page-name}/{page-name}.spec.ts`
  - Prefer `data-testid` selectors
  - Test documentation in `{page-name}.md`
  - Never assume UI structure — explore with MCP first

### pytest
- **Trigger**: Writing Python tests — fixtures, mocking, markers
- **Path**: `~/.config/opencode/skills/pytest/`
- **Rules**:
  - Class grouping for related tests (`TestUserService`)
  - `@pytest.fixture` for reusable setup/teardown
  - `pytest.raises(ValueError, match="...")` for expected errors
  - `conftest.py` for shared fixtures
  - `@pytest.mark.*` for categorization
  - `mocker` fixture (from pytest-mock) for mocking

### react-19
- **Trigger**: Writing React components — no useMemo/useCallback needed
- **Path**: `~/.config/opencode/skills/react-19/`
- **Rules**:
  - No manual memoization — React Compiler handles optimization
  - Named imports from `"react"` only, never `import React`
  - Server Components by default
  - `use()` for promises/context
  - `useOptimistic()` for optimistic updates
  - `useActionState()` for form actions

### tailwind-4
- **Trigger**: Styling with Tailwind — cn(), theme variables, no var() in className
- **Path**: `~/.config/opencode/skills/tailwind-4/`
- **Rules**:
  - Never `var()` in className — use semantic Tailwind classes
  - Never hex colors in className — use Tailwind color classes
  - Dynamic numeric values → `style` prop
  - Conditional styles → `cn("base", condition && "variant")`
  - Static styles only → plain `className` (no cn needed)
  - `@theme` directive for design tokens in CSS

### typescript
- **Trigger**: Writing TypeScript code — types, interfaces, generics
- **Path**: `~/.config/opencode/skills/typescript/`
- **Rules**:
  - Const types pattern: `as const` + `keyof typeof` — never raw union strings
  - Flat interfaces: max 1 level deep; nested objects → dedicated interface
  - `satisfies` for type validation without widening
  - Prefer `interface` over `type` for object shapes
  - Discriminated unions with `kind`/`type` field
  - Generic constraints with `extends`

### work-unit-commits
- **Trigger**: Implementation, commit splitting, chained PRs, keeping tests and docs with code
- **Path**: `~/.config/opencode/skills/work-unit-commits/`
- **Rules**:
  - One commit = one deliverable work unit (behavior, fix, migration, docs)
  - Keep tests with the code they verify — same commit
  - Keep docs with the user-visible change — same commit
  - Reviewer should understand each commit's purpose from diff + message
  - Each commit should be a viable chained PR candidate
  - SDD >400 line forecast → pre-slice into chained PRs before implementing

### zod-4
- **Trigger**: Using Zod for validation — breaking changes from v3
- **Path**: `~/.config/opencode/skills/zod-4/`
- **Rules**:
  - Top-level validators: `z.email()`, `z.uuid()`, `z.url()` (not chained from `z.string()`)
  - `z.string().min(1)` replaces `nonempty()`
  - `{ error: "..." }` in object options replaces `.required_error()`
  - `z.pipeline()` for sequential transforms
  - `z.instance()` replaces `z.instanceof()`
  - Branded types via `z.brand()`

### zustand-5
- **Trigger**: Managing React state with Zustand
- **Path**: `~/.config/opencode/skills/zustand-5/`
- **Rules**:
  - `create<T>((set) => ({...}))` pattern with explicit interface
  - Immer middleware (`immer`) for nested state mutations
  - Persist middleware from `zustand/middleware` for persistence
  - Selector-based subscriptions for render optimization
  - No default export — named export `useXStore`

## Convention Files

### AGENTS.md
- **Path**: `C:\autobus\fleetops-pro\AGENTS.md`
- **Content**: Project overview, stack, file structure, API reference, DB schema, dev commands, design notes
- **Referenced in registry**: Yes
- **Available for skill resolution**: Yes
